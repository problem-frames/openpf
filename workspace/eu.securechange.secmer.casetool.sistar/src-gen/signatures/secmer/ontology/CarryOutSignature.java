/*******************************************************************************
 * EMF Specific pattern signature of the secmer.ontology.carryOut pattern
 * Generated by EMF-IncQuery
 *******************************************************************************/

package signatures.secmer.ontology;

import org.eclipse.emf.ecore.EObject;
import org.eclipse.emf.ecore.EStructuralFeature;
import org.eclipse.viatra2.compiled.emf.runtime.api.IPatternSignature;

/**
 * Domain specific Data transfer Object of the secmer.ontology.carryOut pattern.
 * Class fields correspond to parameters of the pattern. Fields with value null are considered unassigned.
 * Each instance is a partial substitution of pattern parameters, 
 * usable e.g. to represent a match of the pattern.
 */
public class CarryOutSignature implements IPatternSignature {

	//constructor
	public CarryOutSignature(Object Actor, Object Process) {
		this.Actor = Actor;
		this.Process = Process;	
	}

	//private attributes
	private Object Actor;	private Object Process;
	
	//getter methods
	public Object getActor(){
		 return Actor;
	}

	public Object getProcess(){
		 return Process;
	}
	
	//setter methods
	public void setActor(Object Actor){
		 this.Actor=Actor;
	}
	public void setProcess(Object Process){
		 this.Process=Process;
	}

	//overridden hashCode() and equals() with Tuple-semantics
	@Override
	public boolean equals(Object obj) {
		if (this == obj)
			return true;
		if (obj == null)
			return false;
		if (getClass() != obj.getClass())
			return false;
		CarryOutSignature other = (CarryOutSignature) obj;
		if (Actor == null) {if (other.Actor != null) return false;}
		else if (!Actor.equals(other.Actor)) return false;
		if (Process == null) {if (other.Process != null) return false;}
		else if (!Process.equals(other.Process)) return false;
		return true;
	}
	
	@Override
	public int hashCode() {
		final int prime = 31;
		int result = 1;
		result = prime * result + ((Actor == null) ? 0 : Actor.hashCode());
		result = prime * result + ((Process == null) ? 0 : Process.hashCode());
		return result;
	}

	@Override
	public String toString(){
		EStructuralFeature feature = null;
		String _temp = " A(n) carryOut signature object ("+hashCode()+")\n";
		// Checks that Actor has a name attribute and uses if it has 
		_temp += "\tActor = ";
		feature = null;
		if(Actor instanceof EObject)
			feature = ((EObject)Actor).eClass().getEStructuralFeature("name");

		if(feature != null && ((EObject)Actor).eGet(feature) != null) _temp += ((EObject)Actor).eGet(feature).toString();
		else _temp += Actor.toString();
		_temp += "\n";

		// Checks that Process has a name attribute and uses if it has 
		_temp += "\tProcess = ";
		feature = null;
		if(Process instanceof EObject)
			feature = ((EObject)Process).eClass().getEStructuralFeature("name");

		if(feature != null && ((EObject)Process).eGet(feature) != null) _temp += ((EObject)Process).eGet(feature).toString();
		else _temp += Process.toString();
		_temp += "\n";

		return _temp;
	}

	@Override
	public Object[] toArray() {
		return new Object[] {Actor, Process};
	}
}
