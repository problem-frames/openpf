package uk.ac.open.problem.diagram.edit.part;

import org.eclipse.gmf.runtime.notation.View;
import uk.ac.open.problem.diagram.edit.parts.NodeEditPart;

public class NodeEditPartOverride extends NodeEditPart {

	public NodeEditPartOverride(View view) {
		super(view);
		// FIXME
//		if (node == null) return;
//		String name = node.getName();
//		if (name != null)
//			System.out.println(name);
	}
}
