To use the decreasoner, depending on your OS platform, please copy the
corresponding files in the binary folder here: 

In Windows:
  copy /s binary\win32\* .

In Linux:
  cp -r binary/linux.x86_64/* .
or 
  cp -r binary/linux.x86/* .

In Mac OSX:
  cp -r binary/macosx.x86_64/* .
